.env
.env.local
.env.*.local
# .env.example is NOT ignored — it's tracked as a template

# Windows Zone.Identifier metadata files
*:Zone.Identifier
*Zone.Identifier
node_modules/
.next/
data/
uploads/
*.db

# Ollama model directories (root-owned, not part of app)
ollama-*-models/
ollama-*-entrypoint.sh

# Manual update backups (update.sh)
.backups/

